interface:
  display_name: 'Build an XR Blocks app'
  short_description: 'Build an XR app ready for simulator or device testing'
  default_prompt: 'Use $xb-build-app with its required $xb-implement foundation to implement this app and prepare its simulator or XR test handoff.'
