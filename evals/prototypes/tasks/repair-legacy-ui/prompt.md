This XR Blocks app stopped working after the SDK update. Replace its old
`enableUI()`, `SpatialPanel`, `TextView`, and `TextButton` setup with the current
built-in UI. Show a world-space status card with a button. Pressing the button
must change the status text from “Ready” to “Activated”. Return the repaired
`main.js`.
